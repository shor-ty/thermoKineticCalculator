#!/bin/bash
#
# Environment set-up for bash shell
#------------------------------------------------------------------------------
export TKC_PROJECT_DIR=$(cd $(dirname ${BASH_SOURCE:-$0})/.. && pwd -P)


#------------------------------------------------------------------------------
